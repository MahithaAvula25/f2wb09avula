extends layout

block content
  h1= title
  p Rows = #{query.rows} and Columns = #{query.cols}
    -
     rows= Number(query.rows)
      col= Number(query.cols)
    if !Number.isInteger(rows)
        p you mentioned rows are not accurate 
    else if !Number.isInteger(col)
        p you mentioned columns are not appropriate 
    else if rows < 1 || rows > 10
        p you mentioned rows are  not in one to ten range 
    else if col < 1 || col > 10
        p you mentioned colums are  not in one to ten range
    else
       table           
            - for(let i=0; i<rows; i++) {
               tr#middle
                 - for(let j=0; j<col; j++) {
                     td= 1+i+j*rows
                     td=" "
                     td=" "
                     td=" "
                 -}              
            - }